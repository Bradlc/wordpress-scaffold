body {
	background: colorBg
	color: colorText
	font-family: fontFamily
	font-size: fontSize
	line-height: lineHeight
}

img {
	max-width: 100%
}

.container {
	max-width: containerWidth + gutterWidth
}